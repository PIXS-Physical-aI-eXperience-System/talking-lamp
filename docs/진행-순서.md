# Talking Lamp 진행 순서

**팀 PIXS** | v0.4 | [파트-분배.md](파트-분배.md) v0.6 기준

> 날짜가 아니라 **순서**로 정리한 문서. 기간이 확정되면 이 순서 위에 주차를 얹으면 된다.
> ★ 표시는 **다른 파트가 기다리는 항목** — 늦어지면 그 파트가 멈춘다.
> ✅ 표시는 **완료된 항목**이다.

> **v0.4 변경** — 2보드(Pi 5 + Jetson) 구성 반영, VLM·TTS 모델 확정에 따른 완료 표시, 웨이크워드 교체(자체 학습)와 그에 따른 **호출어 결정 인계(A → C)** 신설, 마이크 도착 후 측정 절차 반영.

---

## 1. 전체 단계

| 단계 | 내용 | 이 단계가 끝났다는 기준 |
| --- | --- | --- |
| **0. 착수** | LeLamp 원본 검증 + 아키텍처·부품·모델 확정 | **LeLamp 원본 조립·MuJoCo 시뮬레이션 구동 확인**(전원 참여), VLM 후보 결정, 하드웨어 부품 발주 완료, 런타임 스키마 확정 |
| **1. 단독 동작** | 각 파트가 혼자 돌아가는 상태 | 음성 왕복 / 검출→타겟점 / 서보 궤적(시뮬레이션 또는 실물) / 기성 보드 결선·구동이 각각 성공 |
| **2. 1차 통합** | 파이프라인 관통 | **S2**(호명 → 시선 → 대화)가 처음부터 끝까지 동작 |
| **3. 전 시나리오** | 기능 완성 | S1·S3·S4·S5·S6 모두 동작 |
| **4. 완성** | 외형·시연 | 조립 완료, 시연 영상 촬영, 출품 준비 |

---

## 2. 파트별 순서

### E · 이수혁 (움직임+하드웨어+IK엔진) — **가장 먼저 움직여야 하는 파트**

원래 모션과 하드웨어는 다른 담당자였지만, 이제 한 사람이 둘 다 맡는다. **자체 PCB 설계를 빼고 기성 부품 조합으로 가기로 하면서 순서가 크게 단순해졌다** — 발주 리드타임이 임계 경로에서 사라졌고, "회로 설계하느라 모션 개발이 멈추는" 구간도 없어졌다. 부품은 초반에 한 번 주문해두고, 배송을 기다리는 동안 시뮬레이션에서 모션 개발을 계속하면 된다.

| 순번 | 작업 | 비고 |
| --- | --- | --- |
| 1 | LeLamp 3D 파일(`.3mf`)·OnShape CAD·조립 문서 확보 | |
| 2 | ✅ LeLamp + lelamp_runtime 클론 → **MuJoCo 시뮬레이션 구동 확인** | 하드웨어 0개로 착수 |
| 3 | **★ 부품 선정·발주** — 기성 버스 서보 드라이버 보드, 서보용 전원 공급기, 서브 MCU 개발보드, 퓨즈 | 서보(12V)·마이크(XVF3800)는 발주 완료. **배송 대기 동안 6~10번을 진행** |
| 4 | 범용 서보 드라이버로 **원본 그대로 1세트 출력 → 조립·서보 구동 검증** | **개조 전 기준점 확보** |
| 5 | 서보 통신 계층 포팅 확인 (`feetech-servo-sdk`+`lerobot` 재사용) | 서보 1개만 있으면 실물 검증 가능 |
| 6 | ✅ 100 Hz 궤적 생성기 — LeLamp 30fps에서 상향 | 속도·가속·저크 제한 포함. 속도 제한값은 무게 실측 후 재조정 |
| 7 | ✅ 역기구학(IK) — 5축 해, 가동범위·특이점 처리 | 시뮬레이션에서 검증 완료 |
| 8 | ✅ 모션 블렌더 (우선순위·관절 마스크·전환 규칙) | 시뮬레이션에서 검증 완료 |
| 9 | ✅ L1 칼만 필터 추종 (얼굴 좌표 + 소리 방향 양쪽 입력 지원) | 실제 검출 결과 연결은 D·C 산출물 대기 |
| 10 | ✅ L0 아이들 모션 (호흡·미세 시선) | **여기까지가 "실물 없이 되는 일" — 완료** |
| 11 | **실물 서보 구동 계층** — 시뮬레이션 백엔드를 실제 서보 버스로 교체 | 서보 입고 후. 시뮬레이션에서 검증된 스택 아래에 붙인다 |
| 12 | 헤드 무게 예산 확정 (조명·방열 추가분 반영) | 서보 전압은 12V로 확정됨 |
| 13 | 전원 구성 확정 — **Jetson 19V + Pi 5 + 서보 12V 분리**, 공통 접지·퓨즈·인러시 대책 | 회로 설계가 아니라 기성품 조합·배선으로 해결 |
| 14 | **보드 간 이더넷 직결 구성** (고정 IP) + **안전 정지 경로 검토** | Pi가 멈추면 서보를 멈출 수단이 없다 — 서브 MCU에 watchdog 검토 |
| 15 | LampBase 재설계(OnShape 개조) — **Pi 5 + Jetson** + 히트싱크 + 기성 보드 여러 장이 들어가도록 확대 | 보드가 하나 더 늘어 공간이 더 빠듯. 부족 시 외부 유닛 분리 검토 |
| 16 | 작업 조명 하우징 설계 | D(비전)의 광학 요구사항 필요 |
| 17 | 개조분 3D 출력 → Pi + Jetson + 기성 보드로 재조립 → 서보 구동 재확인 | |
| 18 | 헤드 무게·토크 실측(조명 포함) → **본인의 모션 속도·가속 한계에 반영** | 예전엔 "D가 E를 기다리는" 인계였지만, 이제는 본인 내부 순서 문제 |
| 19 | 조명 모듈 제작·방열 | |
| 20 | 외형 조립·**배선 정리** | 보드가 여러 장이라 배선량이 많다. 전원 참여 |
| 21 | 마감·보강 (무게추/스프링 등) | |
| 22 | 모션 저작 도구, 프리미티브 라이브러리 보완, 모션 품질 튜닝 | **끝이 없는 작업 — 시간 날 때마다 계속** |

### B · 시스템통합 (담당: 김태현) — **런타임 골격이 1번**

런타임 골격이 먼저다. 이게 늦으면 A·C·D·E가 각자 따로 만들고, 통합 때 전부 뜯어고치게 된다.

| 순번 | 작업 | 비고 |
| --- | --- | --- |
| 1 | 이벤트 스키마 정의 + 런타임 골격 설계 | |
| 2 | **★ 런타임 스텁 배포** | A·C·D·E가 이걸 상대로 개발 시작 |
| 3 | 시나리오 중재기 (S1~S6 우선순위) | |
| 4 | barge-in(말 끊기) 조율 — A(인지) 요청 취소 + E(이수혁) 모션 인터럽트를 동시에 발행 | TTS 정지 자체는 C(음성)가 감지·실행 |
| 5 | 지연 예산 계측·배분 | |
| 6 | 통합 조율 | 후반 내내 |

### A · 인지 (담당: 주경태) — **벤치마크가 1번**

VLM이 Jetson에서 얼마나 느린지를 모르면 메모리 예산도, 모델 크기도 못 정한다. 여기서 결과가 나빠야 아키텍처를 바꿀 시간이 있다.

| 순번 | 작업 | 비고 |
| --- | --- | --- |
| 1 | ✅ **VLM 후보 벤치마크** → **InternVL3.5-2B 확정** | 실측 피크 3.22GB. SmolVLM은 한국어 질문에 영어로 답하고 메모리도 더 커서 탈락 |
| 2 | **★ 호출어(램프 이름) 결정 [시급]** | **C가 웨이크워드 학습을 시작하려면 필요.** 학습 후 변경 어려움 |
| 3 | 응답 속도(첫 토큰까지) 측정 | 메모리만 재고 속도는 아직 |
| 4 | 라이선스 백본까지 확인 + Qwen3-VL-2B 탈락 근거 기록 | 저장소가 GPL-3.0이라 호환 여부도 확인 |
| 5 | **언어 혼입 후처리 필터** | 중국어·일본어 단어 혼입 관찰됨. **C의 TTS가 한국어·영어만 처리하므로 안 거르면 음성 합성이 깨진다** |
| 6 | 온보드 구동 + 양자화 최적화 | |
| 7 | **★ 행동 태그 스키마 + 파서** | B(시스템통합)가 라우팅에, E(이수혁)가 재생에 필요. 대사와 행동 배열을 분리한 JSON |
| 8 | 시스템 프롬프트·페르소나 설계 | 호출어와 어울리게 |
| 9 | S3 자연어 조명 의도 파싱 | |
| 10 | 시나리오 평가셋 구축 | 프롬프트 수정 시 회귀 검증용 |
| 11 | **S5 발화 억제 정책** | 나머지가 다 돌아야 판단 가능 → 마지막 |
| 12 | 프롬프트 튜닝 | |

### C · 음성 (담당: 최승원)

| 순번 | 작업 | 비고 |
| --- | --- | --- |
| 1 | ✅ TTS 엔진 후보 한국어 음질 실측 → **MeloTTS ONNX int8 확정** | STT 포함 피크 1.41GB. torch 제거로 2.0GB에서 감축 |
| 2 | ✅ 웨이크워드 라이선스 검증 → **`pvporcupine` 사용 불가 판정** | 유료 전환 + 인터넷 키 인증. 영어 호출어 대안도 오작동으로 탈락 |
| 3 | Jetson용 실행 환경 빌드 (ONNX 런타임) | 도커로 빌드 중 |
| 4 | STT 온보드 구동 | |
| 5 | **웨이크워드 자체 학습** (openWakeWord) + VAD | **A의 호출어 결정을 기다림** — 학습 후 변경 어려움 |
| 6 | 마이크(XVF3800) 도착 → 채널 수·출력 경로 확인 → **정면 보정** | 여기서 걸리면 나머지가 무의미하다 |
| 7 | **★ DOA(소리 방향) 측정 → E에 공급** | S6 입력. 조용할 때·팬·서보 조건별 8방향 측정 → **가장 나쁜 조건의 오차를 E의 칼만 측정 노이즈로 전달** |
| 8 | AEC 실측 — 내장 AEC로 충분한지 판정 | 하드웨어 AEC 내장으로 부담 감소. far-end reference 경로 확인이 선결 |
| 9 | 스트리밍 처리 (첫 음절까지 시간 단축) | |
| 10 | barge-in 감지 및 TTS 즉시 정지 | 이후 조율(A 취소·E 인터럽트)은 B(시스템통합)가 이어받음 |

### D · 비전 (담당: 김아현)

광학 요구사항만 순서를 앞당긴다. E(이수혁)가 조명 모듈을 만들려면 이 숫자가 있어야 한다.

| 순번 | 작업 | 비고 |
| --- | --- | --- |
| 1 | 검출 모델 후보 선정 (얼굴/사물 분리 여부 결정) | |
| 2 | 카메라 캘리브레이션 절차 수립 | |
| 3 | **★ 조명 광학 요구사항 산출 → E 전달** | 순서 앞당김. E가 대기 중 |
| 4 | 검출기 Jetson 배포·최적화 | |
| 5 | 책상 평면 등록 방식 구현 | |
| 6 | 2D → 3D 좌표 환산 | |
| 7 | **★ 3D 타겟점 인터페이스 → E 전달** | S1의 입력 |
| 8 | 조명 배치 최적화 (조도 최대 / 눈부심·그림자 최소) | |
| 9 | S4 자리 비움·복귀 판정 | 히스테리시스 설계 |
| 10 | 검출 실패 폴백 | |
| 11 | 튜닝 | |

---

## 3. 파트 간 인수인계 순서

순서대로 나와야 뒤가 막히지 않는다.

| 순번 | 넘기는 쪽 | 받는 쪽 | 산출물 | 늦어지면 |
| --- | --- | --- | --- | --- |
| 1 | ✅ **A** | 전체 | VLM 벤치마크 결과 | — (완료: InternVL3.5-2B, 3.22GB) |
| 2 | ✅ **E** | 전체 | **공통 규약** (좌표계·관절 순서·단위) | — (완료: [파트-분배.md](파트-분배.md) 2.4절) |
| 3 | **A** | C | **호출어(램프 이름)** | C가 웨이크워드 학습 착수 못 함. 학습 후엔 변경 어려움 |
| 4 | **B** | A·C·D·E | 런타임 스텁 + 이벤트 스키마 | 각자 따로 만들다 통합 때 충돌 |
| 5 | **D** | E | 조명 광학 요구사항 (광량·배광각·색온도) | E가 조명 모듈 발주 못 함 |
| 6 | **E** | — | 하드웨어 부품 발주 | 배송 지연 = 실물 검증 지연 |
| 7 | **A** | B·E | 행동 태그 스키마 | B가 라우팅 못 하고 E가 태그 재생 못 함 |
| 8 | **A** | C | 언어 혼입 필터 규칙 | 중국어 문자가 TTS로 넘어가 음성 합성이 깨짐 |
| 9 | **D** | E | 3D 타겟점 인터페이스 | S1 조명 배치 불가 |
| 10 | **C** | E | 소리 방향(DOA) + **측정 오차**(칼만 노이즈 파라미터) | S6 불가, E가 칼만 튜닝 못 함 |

> 예전엔 "E(하드웨어)→D(모션) 헤드 무게·토크 실측값"도 인계 항목이었지만, 이제 둘 다 이수혁 한 명이라 **인계가 아니라 본인 작업 순서 문제**가 됐다 (2절 E 파트 16번 참고).

---

## 4. 통합 순서

시나리오를 한꺼번에 붙이지 말고 아래 순서로 하나씩 관통시킨다.

| 순번 | 대상 | 참여 | 왜 이 순서인가 |
| --- | --- | --- | --- |
| 1 | **S2** 호명 → 시선 → 대화 | B·C·A·E | 음성→인지→모션 **전 구간을 관통하는 최소 시나리오**. 여기서 막히는 건 전부 구조 문제 |
| 2 | **S1** 조명 자동 배치 | D·E | 조명 하드웨어가 나온 뒤에 가능 |
| 3 | **S3·S4** 자연어 조명 제어 / 자리 비움 | A·D | S1·S2가 돌아야 위에 얹힘 |
| 4 | **S6** 소리 방향 추종 | C·E | AEC·DOA가 안정된 뒤 |
| 5 | **S5** 상황 인식 자연 발화 | A | 전부 돌아가야 "적절한 타이밍"을 판단할 수 있음 |
| 6 | 전체 리허설 → 시연 촬영 | 전원 | |

---

## 5. 순서상 주의점

- **부품은 초반에 한 번에 주문한다.** 자체 PCB를 만들지 않으므로 발주 리드타임 리스크는 사라졌지만, 부품이 없으면 실물 검증을 못 한다. 배송을 기다리는 동안 시뮬레이션 작업(2절 E 파트 6~10번)을 진행하면 대기 시간이 낭비되지 않는다.
- **LeLamp 원본 검증(E 4번)을 건너뛰지 말 것.** 원본이 동작하는 걸 먼저 확인해야, 이후 문제가 생겼을 때 개조 탓인지 원본 탓인지 가릴 수 있다.
- **배선 정리를 얕보지 말 것.** PCB 한 장으로 통합하지 않은 대신 베이스 안에 보드가 여러 장 들어간다. Jetson + 히트싱크만으로도 공간이 빠듯한 상황이라, LampBase 재설계(E 13번) 단계에서 실제 부품 치수를 재서 배치를 확정해야 한다.
- **B의 런타임 스텁이 A·C·D·E의 출발선이다.** 완성품이 아니라 껍데기라도 먼저 나와야 한다.
- **AEC와 모션 튜닝은 예상보다 오래 걸린다.** 이 둘은 순서상 뒤에 있지만 착수는 앞당기는 게 안전하다.
- **S5는 마지막에 붙인다.** 다른 게 다 돌아가야 "언제 말을 걸어야 자연스러운지"를 판단할 수 있고, 잘못 발동하면 인상이 크게 나빠진다. 오프 스위치를 함께 만든다.
- **조립·배선·무게 실측은 전원 참여.** 이수혁 담당이지만 이 구간은 손이 많이 필요하다.
