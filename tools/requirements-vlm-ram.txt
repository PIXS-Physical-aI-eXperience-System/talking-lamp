# Tested with torch 2.8.0+cu128 in the existing PC CUDA container.
# Jetson requires its own compatible torch/CUDA installation.
# Install torchvision==0.23.0 from https://download.pytorch.org/whl/cu128
# with --no-deps alongside the existing torch 2.8.0+cu128 first.
transformers==4.57.6
bitsandbytes==0.48.2
accelerate==1.12.0
Pillow==12.3.0
psutil==7.2.2
