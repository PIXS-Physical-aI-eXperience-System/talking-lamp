# 웨이크워드 "픽스야" 1차 학습 결과

2026-09-17 / 파트 C(음성) / 최승원

## 지금 상태 (2026-09-23)

| | 값 | 어디서 |
| --- | --- | --- |
| 동작 지점 | **연속 3창, 임계 0.7** | 실제 마이크 헛깨움으로 정함 |
| 팀원 목소리 깨어남 | 17/17 | 실제 마이크, 최고 점수 기준. 일부러 이상하게 말한 3번 제외. **연속 3창 기준 실측은 없다**(파일 유실) — 노트북 녹음 기준 99% |
| 학습 밖 목소리 깨어남 | **42%** | 실제 마이크, 홀드아웃 모델 |
| 대화 중 헛깨움 | 0.40회/분 | 실제 마이크 5분 |

**팀원이 부르는 데모에는 쓸 수 있고, 남이 부르면 절반 넘게 안 깨어난다.**

노트북 녹음으로 잰 숫자(깨어남 94%, 문장 헛깨움 3%)는 실제 마이크에서
유지되지 않았다. 학습 밖 목소리가 95% → 63% 로 떨어졌는데(같은 모델·
같은 사람) 채널만 달랐다 — XVF3800 을 거친 소리로 학습하지 않은 탓이다.
자세한 것은 아래 "실제 마이크 실측" 절.

아래 "데이터"~"합성 부정" 은 09-17 에 노트북 녹음으로 잰 기록이다.

## 데이터

5명 × (호출어 20 + 헷갈리는 말 12 + 평범한 문장 6 + 방 소리 10초).

| 사람 | 호출어 | 부정 | 방 소리 |
| --- | --- | --- | --- |
| 주경태 | 20 | 18 | 있음 |
| 김아현 | 20 | 18 | 있음(-57 dB, 조용한 방) |
| 김태현 | 20 | 18 | 있음 |
| 이수혁 | 20 | 18 | **-240 dB, 뺌** |
| 최승원 | 20 | 18 | 있음 |

이수혁 방 소리는 완전한 디지털 무음이다. 맥이 첫 녹음에서 마이크 권한을 묻느라
그 10초가 통째로 빈 것으로, 우리가 문서에 적어 둔 그 실패가 그대로 재현됐다.
목소리 녹음은 멀쩡하므로(-35 dB) 뺀 것은 방 소리 하나뿐이다.

## 어떻게 학습했나

openWakeWord 와 같은 구조다. 구글 speech_embedding(고정) 위에 작은 분류기만
얹는다. 임베딩을 학습하지 않으므로 녹음 190개로도 분류기는 학습된다.

창은 2.0초다. 이 길이가 임베딩 (16, 96) 을 내놓고, 그게 분류기 입력 모양이다.

녹음 190개를 7,400창으로 부풀렸다(`bench/wake_augment.py`) — 창 안 위치,
크기 -12~+4 dB, 방 소리 SNR 3~25 dB, 말 빠르기 0.9~1.12배.

**뒤가 잘린 호출어를 부정으로 넣었다.** 실제로는 창이 80 ms 씩 밀려가므로
"픽스"까지만 들어온 창이 반드시 생긴다. 이걸 가르치지 않으면 말이 끝나기 전에
깨어난다.

## 어떻게 쟀나

**한 사람 빼고 학습(LOPO).** 4명으로 학습하고 나머지 한 명 녹음으로 시험한다.
이게 유일하게 정직한 숫자다 — 학습에 쓴 목소리로 재면 99.8%가 나오지만
심사 때 부르는 사람은 학습에 없던 목소리다.

증강하지 않은 **원본 녹음**을 실제와 같이 80 ms 씩 흘려보낸다(`bench/wake_eval.py`).
창 하나당 비율이 아니라 **부른 횟수 기준**으로 센다.

## 결과

| 연속 | 임계 | 깨어남 | 헷갈리는 말 | 평범한 문장 | 방 소리 |
| --- | --- | --- | --- | --- | --- |
| 1창 | 0.5 | 98% | 32% | 13% | 0회/분 |
| 1창 | 0.9 | 97% | 25% | 3% | 0회/분 |
| **2창** | **0.7** | **94%** | **20%** | **3%** | **0회/분** |
| 2창 | 0.9 | 91% | 18% | 3% | 0회/분 |
| 3창 | 0.9 | 85% | 12% | 0% | 0회/분 |

(09-17) **연속 2창, 임계 0.7** 로 정했다 — 이후 실제 마이크 실측을 보고
**연속 3창** 으로 올렸다. 깨어남 95% 아래로 떨어지지 않으면서 헛깨움이
가장 낮은 자리다. `voice/wake.py` 의 기본값이다.

연속을 요구하는 이유: 한 창만 넘어도 깨우면 스치는 소리에 깨어난다. 호출어는
여러 창에 걸쳐 있으므로 진짜는 연속으로 넘는다. 헷갈리는 말 헛깨움이 35% → 18%
로 절반이 되는 동안 깨어남은 99% → 96% 밖에 안 떨어졌다.

### 숫자를 얼마나 믿을 수 있나

| | 센 것 | 95% 구간 |
| --- | --- | --- |
| 깨어남 | 94/100 | 88~97% |
| 헷갈리는 말 | 12/60 | 12~32% |
| 평범한 문장 | **1/30** | **1~17%** |

**문장 헛깨움 3%는 문장 30개에서 1개가 걸린 것이다.** 실제 값이 1%일 수도
17%일 수도 있다. 30개로는 그 이상 못 좁힌다.

### 사람별 (연속 2창, 임계 0.7)

| 사람 | 깨어남 | 헷갈리는 말 | 문장 |
| --- | --- | --- | --- |
| 주경태 | 95% | 17% | 0% |
| 김아현 | 100% | 50% | 0% |
| 김태현 | 100% | 25% | 0% |
| 이수혁 | 95% | 17% | 17% |
| 최승원 | 95% | 25% | 0% |

사람마다 크게 갈린다. 5명으로는 부족하다는 표시다.


## 합성 부정 — 효과가 있었다

사람한테 대화 녹음을 더 받는 대신 합성 음성으로 부정 문장을 만들어 봤다
(`bench/wake_tts_neg.py`, 207문장).

- [X]스야 꼴 — "그거 박스야", "이게 주스야?" 처럼 앞 음절만 다르고 뒤가 같은 것
- 픽- 으로 시작하는 말 — 픽셀, 픽업, 픽션, 피크, 빅스, 믹서…
- 평범한 대화 40문장
- **램프가 실제로 말할 문장 12개** — 스피커로 나가는 게 그 목소리라
  자기 말에 깨어나면 안 된다

한계: 저장소의 한국어 TTS는 melo·piper·mimic3 셋 다 KSS 라 사실상 한 사람
목소리다. 목소리 다양성은 여기서 못 얻는다. 속도·음높이를 사람 녹음보다
넓게(0.82~1.25배) 흔들어 가짜 화자를 만들었다.

**효과 확인은 진짜 사람 녹음으로만 했다.** 합성은 학습에만 넣고 평가는
건드리지 않았다. LOPO 에서도 시험 쪽으로 새지 않게 `합성` 으로 따로 묶었다.

같은 녹음 하나하나를 두 설정으로 재서 짝지어 비교했다(`bench/wake_compare.py`).
비율만 보면 4/30 이 1/30 이 된 것이라 동전 던지기와 구분이 안 된다.

| | 사람 녹음만 | 합성 추가 | 부호 p | 크기 p |
| --- | --- | --- | --- | --- |
| 호출어 (높을수록 좋음) | 0.990 | 0.983 | 0.332 | 0.195 |
| 헷갈리는 말 (낮을수록) | 0.410 | **0.325** | **0.000** | **0.000** |
| 평범한 문장 (낮을수록) | 0.332 | **0.204** | 0.076 | **0.008** |

**부르는 능력은 안 깎이고(p=0.20) 헛깨움만 내려갔다.**

씨앗 5개를 평균냈다. 같은 씨앗으로 두 번 돌려도 결과가 미세하게 달랐기
때문이다(0.985 vs 0.986) — BLAS 가 여러 스레드로 더하는 순서가 매번 같지
않고 그 차이가 학습 300회를 거치며 벌어진다. 한 번만 재면 그 잡음을 효과로
착각한다.

## 그때 정한 다음 할 일 (09-17)

① **실제 마이크로 재기** — 09-21 에 했다. 아래 "실제 마이크 실측".
노트북 숫자는 유지되지 않았다.

② **부정 녹음을 더 받기** — 사람 대신 실제 마이크 앞에서 혼자 쌓는 쪽으로
바꿨다(`wake_field.py --save-audio`). 아래 "실제 마이크 실측 → 다음".

## 파일

| | |
| --- | --- |
| `bench/wake_augment.py` | 녹음 190개 → 창 7,400개 |
| `bench/wake_train.py` | 학습 + LOPO + ONNX 내보내기 |
| `bench/wake_eval.py` | 부른 횟수 기준 실측 |
| `bench/wake_compare.py` | 설정 둘을 같은 녹음으로 짝지어 비교 |
| `bench/wake_tts_neg.py` | 합성 부정 207문장 만들기 |
| `bench/wake_field.py` | **실제 마이크로 재기** (09-21 실측에 썼다) |
| `models/wake/*.onnx` | 우리 모델 + openWakeWord 특징 추출 모델 2개 |
| `models/wake/pixs-ya.onnx` | 5명 전부로 학습한 최종 모델 (801 KB) |
| `out/wake-feats.npz` | 임베딩 캐시(재학습 시 2분 40초 절약) |

내보낸 ONNX 는 sklearn 과 최대 1.26e-06 차이로 같다(`verify_onnx`). 조용히
틀린 모델을 내보내는 것을 막으려고 매번 확인한다.

20 ms 프레임으로 넣는 실제 경로가 80 ms 덩어리로 잰 측정 경로와 같은 판정을
내는지도 60개 녹음으로 확인했다(불일치 0건). openWakeWord 는 1280 표본이 차기
전에는 직전 점수를 그대로 돌려주므로, 20 ms 프레임을 그냥 넣으면 같은 점수가
네 번 나와서 "연속 2창"이 저절로 충족된다. `voice/wake.py` 가 80 ms 단위로
모은 뒤에 넣는 이유다.

## 실제 마이크 실측 (2026-09-21)

위 숫자는 전부 노트북 녹음이다. XVF3800 → 랜 → 젯슨 경로로 다시 쟀다
(`bench/wake_field.py`). **결과가 많이 다르다.**

| | 노트북 녹음 | 실제 마이크 |
| --- | --- | --- |
| 학습 밖 목소리 깨어남 | 95% | **63%** |
| 대화 중 헛깨움 | (문장 3% 환산 0.45회/분) | **1.20회/분** |

학습에 없는 사람을 구하기 어려워서 거꾸로 했다 — 최승원을 빼고 학습한
모델(`models/wake/pixs-ya-holdout.onnx`)로 최승원이 부른다. 모델 입장에서는
처음 듣는 목소리다. 네 명으로 학습한 모델이라 보수적이다.

**마이크가 소리를 망치는 게 아니다.** 같은 마이크로 학습한 목소리는 20번 중
17번이 정확히 1.000 이었다(나머지 3번은 일부러 이상하게 말한 것). 채널이
바꾸는 것은 **일반화**다 — 노트북 녹음으로 배운 특징이 XVF3800 을 거친 낯선
목소리에는 안 옮겨간다.

임계값으로 풀 문제가 아니다. 실패한 7번의 최고 점수는 0.000 0.015 0.035
0.124 0.175 0.368 0.370 이고 성공한 12번은 전부 0.949 이상이다. **그 사이가
비어 있다.**

### 동작 지점을 연속 3창으로 올렸다

실제 마이크 5분 대화에서:

| 연속 | 임계 | 헛깨움 | 팀원 깨어남(노트북) | 학습 밖(실제 마이크) |
| --- | --- | --- | --- | --- |
| 1창 | 0.7 | 2.20회/분 | 100% | 63% |
| 2창 | 0.7 | 1.20회/분 | 100% | 63% |
| **3창** | **0.7** | **0.40회/분** | **99%** | **42%** |
| 3창 | 0.9 | 0.40회/분 | 98% | 26% |

임계 0.9 는 헛깨움이 같은데 깨어남만 떨어져서 쓰지 않는다.

팀원 목소리는 임계 0.7 을 중앙값 9창 연속으로 넘으므로 3창을 요구해도 거의
안 떨어진다. 학습 밖 목소리는 여유가 없어 크게 떨어진다.

**남은 문제: 학습 밖 목소리 42%.** 이건 그 사람들 목소리 없이는 못 고친다.
지금 물건은 사실상 **"팀원이 부르는 데모"** 에 맞춰져 있다.

### 모델이 "픽" 을 안 듣고 있었다

"스야" 만 말해도 깨어난다는 제보를 받고 쟀다.

| 들려준 것 | 깨어남 |
| --- | --- |
| 픽스야 | 100% |
| "픽" 잘라냄 → 스야 | **99%** |
| 절반 넘게 잘라냄 → 야 | **92%** |

증강 설계 탓이다. 긍정을 만들 때 말의 끝을 창 끝에 맞췄더니 꼬리는 항상
같은 자리에 있고 머리는 빠르기에 따라 흔들려서, 꼬리가 더 쓸 만한 단서가
됐다. 거기에 뒤가 잘린 것을 부정으로 넣어 그 지름길을 더 굳혔고, 앞이 잘린
것은 "애매하다" 며 뺐다.

머리 잘린 호출어를 부정으로 넣으니 "야" 92%→19%, "스야" 99%→72% 로 줄었다
(코드에 반영). 다만 노트북 홀드아웃에서 방 소리 헛깨움이 0→1.5회/분 으로
나빠져서 **모델은 아직 바꾸지 않았다.** 실제 마이크 녹음을 받아 한 번에
다시 학습한다.

호출어 자체도 불리하다. "픽스야" 는 3음절 중 뒤 2음절이 한국어에서 가장
흔한 소리다. "스로 끝나는 말 + 야" 가 대화에 널려 있다. 첫 녹음 요청서에
적어 둔 우려가 그대로 나타났다.

### 다음

**호출어 재녹음은 시간이 없어 못 한다.** 대신 혼자 할 수 있는 것부터 한다.

```bash
venvs/melo-onnx/bin/python bench/wake_field.py --calls 20 --talk-min 20 --save-audio
```

한 번에 세 가지가 쌓인다 — 실제 채널을 거친 호출어 20개(한 사람), 부정
20분, 그리고 **모델이 실제로 헛깨운 2초들**. 마지막 것이 가장 값이 나간다.

부정만 채널이 맞아도 헛깨움은 내려갈 가능성이 높다. 학습 밖 목소리는
그 사람들 목소리 없이는 안 된다.
